# Full UOR-Framework workspace for GitNexus (ontology + clients + codegen + conformance +
# foundation + docs generator + website + macros). Aligns visual analysis with the whole repo.
#
# Upstream `.gitignore` hides `/public/` (uor-build output). Re-include it so generated JSON-LD
# and siblings are indexed when present after `npm run uor:build-ontology`.
!/public/
!/public/**

# Not UOR-authored source
/external/

# Rust build artifacts only
target/
**/target/
